<div class="content-body">
    <section class="welcome-sec">
        <h1>Ponto de Venda (PDV)</h1>
        <p>Registre as saídas de produtos e acompanhe o histórico de vendas recentes.</p>
    </section>
    
    <div class="main-grid">
        <section class="data-card">
            <div class="card-header">
                <h2>Últimas Vendas</h2>
            </div>
            
            <div class="table-container">
                <table class="custom-table">
                    <thead>
                        <tr>
                            <th>Recibo</th>
                            <th>Produto</th>
                            <th>Qtd.</th>
                            <th>Total</th>
                            <th>Pagamento</th>
                            <th>Status</th>
                            <th class="text-center">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each vendas}}
                            <tr>
                                <td><span style="color: var(--text-muted); font-size: 0.85rem;">#{{this._id}}</span></td>
                                <td><strong>{{this.product_name}}</strong></td>
                                <td>{{this.quantity}}x</td>
                                <td><strong>R$ {{this.total_price}}</strong></td>
                                <td>{{this.payment_method}}</td>
                                <td><span class="badge-status status-active">Concluída</span></td>
                                <td>
                                    <div class="d-flex justify-content-center align-items-center gap-2">
                                        <button class="btn btn-outline btn-sm" 
                                            data-bs-toggle="modal" 
                                            data-bs-target="#editModal"
                                            data-id="{{this._id}}"
                                            data-product="{{this.product_id}}"
                                            data-quantity="{{this.quantity}}"
                                            data-payment="{{this.payment_method}}">
                                            Editar
                                        </button>

                                        <form action="/dashboard/sales/delete" method="POST" onsubmit="return confirm('Excluir venda?')" class="m-0">
                                            <input type="hidden" name="sale_id" value="{{this._id}}">
                                            <button type="submit" class="btn btn-danger btn-sm">Remover</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        {{else}}
                            <tr>
                                <td colspan="7" style="text-align: center; padding: 2rem; color: var(--text-muted);">
                                    Nenhuma venda registrada ainda.
                                </td>
                            </tr>
                        {{/each}}
                    </tbody>
                </table>    
            </div>
        </section>

        <section class="data-card">
            <div class="card-header">
                <h2>Nova Venda</h2>
            </div>
            <div id="alert-container"></div>

            <form action="/dashboard/sales/create" method="post">
                <div class="form-group">
                    <label>Produto</label>
                    <select name="product_id" class="form-input" required>
                        <option value="">Selecione...</option>
                        {{#each produtos}}
                            <option value="{{this._id}}">{{this.name}} - R$ {{this.price}}</option>
                        {{/each}}
                    </select>
                </div>
                <div class="form-group">
                    <label>Quantidade</label>
                    <input type="number" name="quantity" min="1" class="form-input" required>
                </div>
                <div class="form-group">
                    <label>Pagamento</label>
                    <select name="payment_method" class="form-input" required>
                        <option value="Pix">Pix</option>
                        <option value="Cartão de Crédito">Cartão de Crédito</option>
                        <option value="Dinheiro">Dinheiro</option>
                    </select>
                </div>
                <button type="submit" class="btn btn-primary w-100 mt-2">Finalizar Venda</button>
            </form>
        </section>
    </div>
</div>

<div class="modal fade" id="editModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form id="editForm" method="POST">
                <div class="modal-header">
                    <h5 class="modal-title">Ajustar Venda</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label>Produto</label>
                        <select name="product_id" id="edit-product" class="form-input" required>
                            {{#each produtos}}
                                <option value="{{this._id}}">{{this.name}}</option>
                            {{/each}}
                        </select>
                    </div>
                    <div class="mb-3">
                        <label>Quantidade</label>
                        <input type="number" name="quantity" id="edit-quantity" class="form-input" required>
                    </div>
                    <div class="mb-3">
                        <label>Pagamento</label>
                        <select name="payment_method" id="edit-payment" class="form-input" required>
                            <option value="Pix">Pix</option>
                            <option value="Cartão de Crédito">Cartão de Crédito</option>
                            <option value="Dinheiro">Dinheiro</option>
                        </select>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="submit" class="btn btn-primary">Salvar</button>
                </div>
            </form>
        </div>
    </div>
</div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('sucess') === 'create') {
            const alertContainer = document.getElementById('alert-container');
            alertContainer.innerHTML = `
                <div class="alert alert-success d-flex align-items-center" role="alert">
                <svg width="20" height="20" fill="currentColor" class="bi flex-shrink-0 me-2" viewBox="0 0 16 16">
                        <path d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0zm-3.97-3.03a.75.75 0 0 0-1.08.022L7.477 9.417 5.384 7.323a.75.75 0 0 0-1.06 1.06L6.97 11.03a.75.75 0 0 0 1.079-.02l3.992-4.99a.75.75 0 0 0-.01-1.05z"/>
                    </svg>
                    <div style="font-size: 0.9rem;">Venda Cadastrada Com Sucesso!</div>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            `;
        }

        else if (urlParams.get('error') === 'quantity') {
            const alertContainer = document.getElementById('alert-container');
            alertContainer.innerHTML = `
                <div class="alert alert-danger d-flex align-items-center alert-dismissible fade show" role="alert">
                    <svg width="20" height="20" fill="currentColor" class="bi flex-shrink-0 me-2" viewBox="0 0 16 16">
                        <path d="M8.982 1.566a1.13 1.13 0 0 0-1.96 0L.165 13.233c-.457.778.091 1.767.98 1.767h13.713c.889 0 1.438-.99.98-1.767L8.982 1.566zM8 5c.535 0 .954.462.9.995l-.35 3.507a.552.552 0 0 1-1.1 0L7.1 5.995A.905.905 0 0 1 8 5zm.002 6a1 1 0 1 1 0 2 1 1 0 0 1 0-2z"/>
                    </svg>
                    <div style="font-size: 0.9rem;">Venda maior que a quantidade do Estoque.</div>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            `;
        }

        const editModal = document.getElementById('editModal');
            if (editModal) {
                editModal.addEventListener('show.bs.modal', event => {
                    const button = event.relatedTarget;
                    
                    // Captura dos dados (Nomes simplificados para não dar erro)
                    const id = button.getAttribute('data-id');
                    const productId = button.getAttribute('data-product');
                    const quantity = button.getAttribute('data-quantity');
                    const payment = button.getAttribute('data-payment');

                    // Preenchimento dos campos usando os IDs que adicionamos
                    editModal.querySelector('#edit-product').value = productId;
                    editModal.querySelector('#edit-quantity').value = quantity;
                    editModal.querySelector('#edit-payment').value = payment;
                    
                    // Ajuste da rota de destino
                    editModal.querySelector('#editForm').action = `/dashboard/sales/update/${id}`;
                });
            }
    </script>